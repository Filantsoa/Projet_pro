{% extends 'base.html.twig' %}

{% block title %}Edit Eleves{% endblock %}

{% block body %}
    <div class="wrapper">
        <div class="section">
            <div class="titre">
                <h1><u>Modification Eleve</u></h1>
            </div>
            <div class="container">
                <div class="row">
                    {{ include('eleves/_form.html.twig', {'button_label': 'Modife'}) }}
                    

                    <a class="btn btn-primary" href="{{ path('app_eleves_index') }}"> << back to list</a>

                    {#{ include('eleves/_delete_form.html.twig') }#}
                </div>
            </div>
        </div>
    </div>
{% endblock %}
