{% extends 'base.html.twig' %}

{% block title %}New Eleves{% endblock %}


    
{% block body %}
    <div class="wrapper">
        <div class="section">
            <div class="titre">
                <h1><u>Nouvelle Eleve</u></h1>
            </div>

            {% for message in app.flashes('success') %}
                <div class="text-center alert alert-success" id="message">
                    {{ message }}
                </div>
            {% endfor %}
            <div class="contanaire">
                <div class="row">
                    {{ include('eleves/_form.html.twig') }}

                    <!-- <a class="btn btn-primary" href="{{ path('app_eleves_index') }}"> << back to list</a> -->
                </div>
            </div>
            
            <div class="container">
                <div class="row">
                    <div class="titre">
                        <h2><u>Liste de(s) eleve(s)</u></h2>
                    </div>
                    <table class="table table-striped table-hover table-bordered">
                        <thead>
                            <th>Matricule</th>
                            <th>Nom</th>
                            <th>Prenom</th>
                            <th>Classe</th>
                            <th>Adresse</th>
                            <th>Telephone</th>
                            <th>Sexe</th>
                            <th id="actionEleve">Actions</th>
                        </thead>
                        <tbody>
                            {% for elefe in eleves %}
                                <tr>
                                    <td>{{ elefe.matricule }}</td>
                                    <td>{{ elefe.nom|upper }}</td>
                                    <td>{{ elefe.prenom|title }}</td>
                                    <td><a href="">{{ elefe.classe|capitalize }} {{ elefe.classe.indice }}</a></td>
                                    <td>{{ elefe.adresse|title }}</td>
                                    <td>+261{{ elefe.telephone }}</td>
                                    <td>{{ elefe.sexe|capitalize }}</td>
                                    <td>
                                        {{ include('eleves/_delete_form.html.twig') }}
                                        <a class="btn btn-info" href="{{ path('app_eleves_show', {'id': elefe.id}) }}"><i class="fa fa-list"></i></a>
                                        <a class="btn btn-warning" href="{{ path('app_eleves_edit', {'id': elefe.id}) }}"><i class="fa fa-edit"></i></a>
                                        <a href="{#{ path('') }#}" class="btn btn-secondary"><i class="fa fa-minus-circle" title="Displine"></i></a>
                                        <a href="{#{ path('') }#}" class="btn btn-success"><i class="fa fa-usd" title="Ecolage"></i></a>
                                    </td>
                                </tr>
                            {% else %}
                                <tr>
                                    <td colspan="13">Aucun enregistrement trouvé</td>
                                </tr>
                            {% endfor %}
                        </tbody>
                    </table>
                    {{ knp_pagination_render(eleves, 'base/pagination.html.twig') }}
                </div>
            </div>
        </div>
    </div> 


    <script>
        // alert("iiv");
        $( document ).ready(function() {
            console.log( "ready!" );
            setTimeout(function(){
                $('#message').css('display', 'none');
            }, 5000);
        });
        // $(window).load(function(){
            
        //     });
    </script>

    
{% endblock %}


        