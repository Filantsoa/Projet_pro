{{ form_start(form) }}
    <div class="row">
        <div class="col-lg-6 col-md-6">
            <div class="form-group">
                {{ form_row(form.nomClasse) }}
            </div>
            <div class="form-group">
                {{ form_row(form.profTitulaire) }}
            </div>
            <button class="btn btn-primary">{{ button_label|default('Enregistre') }}</button>
        </div>
    </div>
    
{{ form_end(form) }}
