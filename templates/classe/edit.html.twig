{% extends 'base.html.twig' %}

{% block title %}Modifie Classe | {{ classe.nomClasse|capitalize }}{% endblock %}

{% block body %}
    <div class="wrapper">
        <div class="section">
            <div class="titre">
                <h1><u>Modifie Classe</u></h1>
            </div>
            <div class="container">
                <div class="row">

                    {{ include('classe/_form.html.twig', {'button_label': 'Enregistre'}) }}

                    <!-- <a href="{{ path('app_classe_index') }}">back to list</a> -->

                    {#{ include('classe/_delete_form.html.twig') }#}
                </div>
            </div>
        </div>
    </div> 
{% endblock %}
