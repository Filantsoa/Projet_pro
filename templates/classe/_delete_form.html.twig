<form method="post" action="{{ path('app_classe_delete', {'id': classe.id}) }}" onsubmit="return confirm('Êtes-vous sûr de bien vouloir supprimer cet élément?');" style="float: right;">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ classe.id) }}">
    <button class="btn btn-danger"><i class="fa fa-trash"></i></button>
</form>
