{% extends 'base.html.twig' %}

{% block title %}Modif Prof Titulaire{% endblock %}

{% block body %}
    <div class="wrapper">
        <div class="section">
            <div class="titre">
                <h1>Modif Prof Titulaire</h1>
            </div>
            <div class="container">
                <div class="row">
                    {{ include('prof_titulaire/_form.html.twig', {'button_label': 'Modifie'}) }}
                    <a class="btn btn-primary" href="{{ path('app_prof_titulaire_index') }}"> << back to list</a>
                    {#{ include('prof_titulaire/_delete_form.html.twig') }#}
                </div>
            </div>
        </div>
    </div>
{% endblock %}
